# Tests run 2026-09-16 (from the book's test log) — every file in this repo was executed in a throwaway vault

| # | What | How | Result |
|---|---|---|---|
| T1 | `capture.zsh` writes a dated log | sourced in `zsh -f`, three commands (one failing, one slow) | `terminal-logs/daily-logs/2026/09/2026-09-16.md` created with header; entries show `✓`/`✗` from exit code and `⏱1s` on the slow one |
| T2 | `sweep.py` | strays in `inbox/`, a flat file in `terminal-logs/daily-logs/`, a name collision, a file in `private/`, an undated note | dry-run lists 2 moves + 1 skip and writes nothing; real run moves 2, skips the collision (both files kept), ignores `private/` and the undated note; second run is a no-op |
| T3 | `summarize.py` | 8-command sample log → `claude -p --allowedTools "" --setting-sources ""` via stdin | 1,186-char report with the three requested headings written to `daily-summaries/2026/09/2026-09-15.md`; refused to run again because the file exists |
| T4 | the report guard | fed "Saved the summary to …", "Done — the report is written.", and a 17-char stub | all three rejected with the reason printed |
| T5 | launchd plist | `plutil -lint` | OK |
| T6 | `claude -p` follows the folder's `CLAUDE.md` | `--setting-sources project --tools ""`, asked for the 3 Sept note | answered `daily/2026/09/2026-09-03.md` from the pattern; asked to read `private/…` it refused, citing the rule |
| T7 | "where did I put that" with read-only tools | `--allowedTools "Read,Grep,Glob"`, asked which note mentions garlic | found `inbox/ideas.md`, quoted the line, and said it excluded `private/` |
| T8 | memory changes behaviour | one-fact file + `MEMORY.md` index + a CLAUDE.md line saying to read it; then proposed the opposite decision | assistant objected, citing the dated decision and its reason |
| T9 | Obsidian Sync enabled on the real vault | `.obsidian/core-plugins.json` | `"sync": true` |

Not tested (narrative only, from the real system): the 2026-08-12 log split, the nine lost weekly reports, the 310-day missing input, the 4-hour clock offset. These are recounted from the real scripts' docstrings and the real summary notes.
| T10 | `/find` slash command (`.claude/commands/find.md`) | `claude -p "/find where does the garlic go"` with `Read,Grep,Glob,Bash(grep:*)`; then `/find zorblax7` where that word exists only under `private/` | first: answered from `memory/MEMORY.md` with the link, date and quoted line, no grep needed; second: "No match … outside `private/`, which I don't search"; no files written by either run |
| T11 | `/morning` slash command (`.claude/commands/morning.md`) | `claude -p "/morning 2026-09-16"` with `Read,Grep,Glob,Write,Bash(date:*)` | wrote exactly `morning/2026/09/2026-09-16.md` with the frontmatter, the 3-column table, three items taken from yesterday's summary and the inbox, and three linked open loops; no other file touched |
| T12 | `weekly.sh` + `prompts/weekly-review.md` | three real daily summaries (13–15 Sept, each produced by T3's `summarize.py`) → `weekly.sh` | wrote `terminal-logs/weekly-reviews/2026/2026-W38.md` with Wins / Open loops / Dropped / Three questions; caught a stash left on a diverged branch and a test case never re-run; second run exits "exists" |
| T13 | `/daily-summary` slash command (chapter 4) | `claude -p "/daily-summary"` with `Read,Grep,Glob,Write,Bash(date:*)` on today's 3-command log | wrote `terminal-logs/daily-summaries/2026/09/2026-09-16.md` with the three sections; worked out the start time of the 1-second command from its end time and duration; no other file touched |
| T14 | `/maintenance` slash command (chapter 6), read-only, six items | planted: a 73-char "Saved…" stub summary for 09-12, a log with no summary for 09-11, a `REJECTED` line in `logs/daily-summary.err.log`, a `SKIP` line in `logs/sweep.out.log`, and a summary file `touch`ed three days after its date | run twice (5-item and final 6-item versions): found every planted fault, plus an orphan summary with no log and a header inconsistency between the two summary writers; flagged the touched file by its round timestamp; proposed a one-line CLAUDE.md fix; nothing written |
| T15 | chapter-3 first conversation | "list the notes in projects/ that have no frontmatter" (read-only), then "add frontmatter to projects/tax-prep.md … change nothing else" (with Edit) | listed the two of three correctly; added exactly the requested frontmatter block, body byte-identical |
| T16 | sub-folder `CLAUDE.md` layering | `clients/one-client/CLAUDE.md` says "confidential" and "first line of any file you write here must be `<!-- local-only -->`"; asked from the vault root to summarise the last call into `decisions.md` there | the file was written with `<!-- local-only -->` as line 1 and the decisions/open items from the call note — the local rule applied without being mentioned in the request |
